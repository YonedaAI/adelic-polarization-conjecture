# Final publication validator brief

Run only after all three papers, the GitHub push and the real Vercel deployment are complete. This is the independent final research-agent gate before the exact final Slack message is sent.

The project root is this repository, not a second nested directory with the same project name. Read the pipeline-validator agent definition and run every check. Its old margin rule conflicts with the canonical paper-format reference: PLAN.md resolves the conflict in favor of the current 11pt letterpaper, margin=1in skeleton. Check that exact current format, not the obsolete instruction. Haskell is explicitly disabled; Lean is the requested formal implementation.

Read the production URL from `.vercel-url`, and the exact pending Slack message from `reviews/slack-final.md`. Do not regenerate or edit the message. Run its literal text through `scripts/slack-lint.sh`. Check the real deployed origin, public HTTP status, and matching project name. Links in the Slack message must use the saved origin and remain on separate lines. The root message links should not be confused with paper-path links in social drafts.

Verify every PNG in `website/public/og/` is 1200x630, at least30000 bytes, and has standard deviation at least0.08. Inspect representative designed images rather than accepting dimensions alone. Require README.md, .knowledge-base.md, website/theme.json, website/public/og/og-default.png, and each paper's LaTeX, PDF, HTML, paper-specific OG and cover. All three papers must contain at least20 pages with substantive mathematical development, not a page count manufactured through padding. Match sources, PDFs, label caches and converted content by actual hashes, using the final verification receipts and live tests rather than an earlier report's status.

Review all three substantive canonical AGY and Codex formatting reports and their fix dispositions, the final independent integration PASS, external Lean code PASS, and external pre-deployment website PASS. Parts I and II have documented, minimal changes after their final formatting reviews: a citation locator and the independent integration review's symbol-only substitutions. The receipts must state these honestly and prove unchanged mathematical content/numbering with current compilation and rendered checks. Do not pretend the final hashes were the external reviewers' original snapshots.

Run `scripts/check-papers.py` and `scripts/check-lean-references.mjs`; read the final style and humanizer records. Check the Lean pin, theorem inventory, axiom audit and current source manifest. The project proves46 explicit theorems in five modules, not a full smooth-test construction, p-adic realization, Hochschild formalization, geometric pairing, Weil positivity or RH. It contains no Haskell implementation. Primary bibliography and final synthesis references need actual source support. Public GitHub must contain the exact published papers and Lean sources, not merely exist as an empty repository.

Run the website's export, source-to-mathematics, conjecture fixture and isolated preview checks. Read final responsive/browser evidence, including synthesis and production, and check evidence hashes against current files. Confirm initial HTML contains all formulas, equation and theorem numbers, MathML/SVG, citation links and anchors. Do not mistake standalone static article pages for absent Next routes. Wide equations must scroll locally without page overflow or equation-tag overlap and remain keyboard reachable after resizing. Both mobile and desktop contents must identify the actual section.

Require twelve local social draft files, one per paper and platform, with actual saved URL, status draft, platform limits and no RH-proof or priority claims. No social account publishing was authorized. Confirm no credentials, dependency caches or supplied context exports were staged or published. Preserve all16 recorded context-file hashes.

Preserve raw external review bytes and their SHA256 receipts. The unrestricted staged whitespace check reports trailing spaces only in raw AGY responses; these were deliberately not reformatted. Authored-file whitespace checking excludes `reviews/*-review*.md` and `reviews/agy-sanity.txt` and passes. Do not silently edit an external review just to remove its whitespace.

Two bounded transition tests were completed after the user's request for proof value. The compact-bump origin-class boundary is in final synthesis Proposition8.3 and received AGY ACCEPT. The logarithmic cokernel-class detector and filling in research/cokernel-survival-attempt.md were checked by the coordinator but remain outside the manuscript reviews and Lean scope. README and research/loop.md must preserve that distinction. No claim of a surviving primitive class or pairing is justified by either test.

Write `reviews/pipeline-validator.md` with explicit PASS/FAIL evidence per category and a final `OVERALL: PASS` or `OVERALL: FAIL`. Do not send Slack, deploy, edit papers, publish social posts, or change repository state. Report concrete failures for root to fix. The final Slack send remains blocked until every actual gate passes.
