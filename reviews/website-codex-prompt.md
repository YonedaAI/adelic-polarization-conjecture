Review the final website in this repository, read-only. Do not edit, deploy, send messages, change Vercel settings, or inspect credentials. End with exactly VERDICT: PASS or VERDICT: NEEDS_FIX. Give concrete file/line findings, severity, and verification performed. The final review must contain at least500 characters.

Scope: website/app, lib, papers.json, public/site.css, public/reading.js, all website/scripts, content metadata, generated out/index.html and out/papers/*/index.html, plus research/static-site-verification.json and research/math-html-review.md when present. Read README.md for the reproducible build contract. Avoid dumping multi-megabyte generated JSON or HTML; parse and inspect relevant elements.

The design deliberately uses a Next.js static-export homepage plus self-contained static HTML article pages copied from public/papers. The articles are not React client components and do not hydrate. Each formula must exist in the initial HTML, including MathML and SVG structure. Do not demand a React ref callback for these standalone articles. Assess the actual implementation.

Check local and cross-page anchors, download targets, source/HTML/PDF freshness, citation and equation/theorem numbering preservation, unsupported LaTeX handling, sanitizer fidelity, metadata and absolute production URLs, fonts/assets, mobile geometry and equation tag spacing, contents navigation, accessibility, injection boundaries, and reproducibility. No analytical or geometric theorem should be presented as Lean-formalized unless research/lean-results.md supports it. The global polarization and RH remain unproved.

Useful checks: from website run node scripts/verify-export.mjs; node scripts/test-article-preview.mjs; node scripts/test-conjecture-preservation.mjs; node scripts/test-math-preservation.mjs local-correspondence polarization-obstructions synthesis. The fixture/preview tests use isolated temporary projects, not publication destinations. Production artifacts have already been built for review. For any rebuild use the actual assigned origin recorded in website/.env.example, not localhost. Inspect actual failures rather than guessing from code-line length or the fact that article routes are absent from Next's app-router route table. Public static files are the intended article route implementation.

Do not treat the existence of a skill checklist as proof that a check passed. Report omissions or unresolved concerns plainly. No tool use outside this project's files and the local build/test commands is needed.
